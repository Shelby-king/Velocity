use linera_sdk::views::{linera_views, MapView, RegisterView, RootView, ViewStorageContext};
use linera_sdk::linera_base_types::{AccountOwner, Amount};
use velocity_dex::VelocityDex;

pub type DexState = VelocityDex;
